#include<iostream>
using namespace std;
struct node
{
	char data[5];
	struct node *next[3];
};
struct stack
{
	char data[5];
	int top;
	int size;
};
create_node(struct node *P)
{
	cout<<"Enter identity : ";
	cin>>a;
	cout<<"Enter number of branches : ";
	cin>>b;
	P->data=a;
	Q[1]=P->data
}
int main()
{
	struct node *L;
	L->data=0;
	L->next=NULL;
	T=L;
	do
	{
		cout<<"Enter identity : ";
		cin>>a;
		cout<<"Enter number of branches : ";
		cin>>b;
		T->data=a;
		for(i=0; i<b; i++)
		{
			T->next[i]=
			cout<<""
		}
	}
}
